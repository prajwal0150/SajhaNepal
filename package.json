{
  "name": "saajha-rahat",
  "version": "1.0.0",
  "private": true,
  "description": "SAAJHA RAHAT — Live Disaster Relief & Needs Mapping Platform for Nepal",
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix frontend run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix frontend run dev",
    "build": "npm --prefix server run build && npm --prefix frontend run build",
    "typecheck": "npm --prefix server run typecheck && npm --prefix frontend run typecheck",
    "seed": "npm --prefix server run seed",
    "test": "npm --prefix server run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
